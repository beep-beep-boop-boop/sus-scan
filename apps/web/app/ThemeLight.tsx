"use client";
import { createTheme } from "@mui/material/styles";

const ThemeLight = createTheme({
  palette: {
    background: {
      default: "#e4f0e2",
    },
  },
});

export default ThemeLight;
